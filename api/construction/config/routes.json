{
  "routes": [
    {
      "method": "GET",
      "path": "/constructions",
      "handler": "construction.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/constructions/count",
      "handler": "construction.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/constructions/:id",
      "handler": "construction.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/constructions",
      "handler": "construction.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/constructions/:id",
      "handler": "construction.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/constructions/:id",
      "handler": "construction.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
